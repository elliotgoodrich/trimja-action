name: CI

on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  initial-testing:
    name: Checks if this action works
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./